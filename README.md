# weatherbot-api

Requirement(s)
-----------

You will need to do `composer install` to install all the dependencies required for this project.

Also, you will need to get an access_key from [Openweathemap](http://openweathermap.org/api) & add it in the index.php file & assign it to `access_key` array item on line #10 in `src/index.php`.